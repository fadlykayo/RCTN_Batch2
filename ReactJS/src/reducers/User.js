const initialState = {
  user: 'fadly',
  email: 'fadly@gmail.com',
};

export default (state = initialState) => {
  // switch (action.type) {
  //   default:
  //     return state;
  // }
  return state;
};
